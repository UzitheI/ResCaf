<div class="menuList">
    {%for dish in dishes%}
    <div class="dish-item">
        <h3>{{dish.name}}</h3>
        <h3>{{dish.price}}</h3>
        <h3>{{dish.description}}</h3>
        <img src="{{dish.image}}" alt=Image>
        <form action="{%url 'menu:addtoCart' dish.id%}" method="post">
            {% csrf_token %}
            <button type="submit">Add to Cart</button>
        </form>
    </div>
    {%endfor%}
</div>
</div>